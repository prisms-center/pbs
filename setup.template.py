from distutils.core import setup
setup(name='pbs', \
      version='VERSION_ID (git sha COMMIT_ID)', \
      description='PBS job submission and management', \
      author='Brian Puchala',\
      author_email='bpuchala@umich.edu',\
      url='REPO_URL', \
      packages=['pbs'])
